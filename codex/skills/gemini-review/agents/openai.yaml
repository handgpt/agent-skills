interface:
  display_name: "Gemini Review"
  short_description: "Advisory Gemini review for bugs, regressions, and safe simplifications"
  default_prompt: "Use $gemini-review to get a concise Gemini review after code changes are complete, including checks for unused code and safe simplification opportunities."
policy:
  allow_implicit_invocation: true
